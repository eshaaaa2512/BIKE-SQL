CREATE DATABASE BIKE_STORE;
USE BIKE_STORE;


--11)Create a new table named "product_category" with columns for category ID and category name..


CREATE TABLE PRODUCT_CATEGORY 
(
     CATEGORY_ID INT,
	 CATEGORY_NAME CHAR(30)
);